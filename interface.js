/**
 * Interfaz: 
 *  -> inputs: (valores de entrada)
 *      -> properties y atributos
 *      -> estilos: css properties, css shadow parts
 *      -> slots
 *  -> outputs: eventos, valores de salida
 * 
 * Api: definida por los metodos publicos de ese componente
 * const el = this.shadowRoot.getElementById('el');
 * el.algunMetodo()
 */